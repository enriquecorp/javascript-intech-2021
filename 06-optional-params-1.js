// Optional parameters
const greeting = function (firstname, lastname, surename) {
  //   if (typeof surename === "undefined") {
  //     surename = "";
  //   }
  return "Hello " + firstname + " " + lastname + " " + surename;
};
greeting("Enrique", "Ortuno");
